# 🎯 Интеграция Онбординга с Backend - Готово!

## ✅ Что было сделано

### 1. **Создана структура типов** (`src/types/index.ts`)
- `OnboardingData` - данные пользователя для отправки на бэкенд
- `CalorieCalculationResponse` - ответ от бэкенда с калориями

### 2. **Добавлены API функции** (`src/lib/api.ts`)
- `calorieApi.calculateDailyCalories()` - расчет калорий
- `calorieApi.saveUserProfile()` - сохранение профиля

### 3. **Создан Context для онбординга** (`src/contexts/OnboardingContext.tsx`)
- Хранит все данные онбординга в одном месте
- Автоматически собирает и валидирует данные
- Предоставляет удобные функции для работы

### 4. **Обновлены страницы онбординга**
- **ActivityLevelPage** → сохраняет `activityLevel`
- **PersonalInfoPage** → сохраняет `gender` и `birthDate`
- **MeasurementsPage** → сохраняет `heightCm`, `weightKg`, `goalWeightKg` с автоконвертацией
- **ResultsPage** → отправляет данные на бэкенд и показывает результат

### 5. **Подключен Provider** в `App.tsx`
- Все страницы имеют доступ к контексту онбординга

## 📡 API Endpoints (требуется на backend)

### POST `/api/calories/calculate`
```json
{
  "activityLevel": "Lightly active",
  "gender": "male",
  "birthDate": "1990-01-15T00:00:00Z",
  "heightCm": 175.5,
  "weightKg": 75.0,
  "goalWeightKg": 70.0
}
```

**Ответ:**
```json
{
  "dailyCalories": 2400,
  "bmr": 1750,
  "tdee": 2400,
  "recommendedProtein": 150,
  "recommendedCarbs": 240,
  "recommendedFat": 80
}
```

### POST `/api/profile/save` (опционально)
Сохраняет профиль пользователя с рассчитанными калориями.

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=https://localhost:7001/api
```

## 🚀 Как это работает

1. Пользователь заполняет данные на страницах онбординга
2. Каждая страница сохраняет данные в `OnboardingContext`
3. На странице `ResultsPage`:
   - Собираются все данные
   - Отправляются на `/api/calories/calculate`
   - Получается ответ с калориями
   - Профиль сохраняется через `/api/profile/save`
   - Показывается результат пользователю

## 🔄 Автоматическая конвертация

Приложение автоматически конвертирует:
- Футы/дюймы → Сантиметры
- Фунты → Килограммы

## 📝 Следующие шаги

1. **Настройте backend endpoints** `/api/calories/calculate` и `/api/profile/save`
2. **Создайте `.env` файл** с правильным URL вашего API
3. **Протестируйте поток онбординга**:
   - `/register` → `/onboarding` → `/goals` → `/activity-level` → `/personal-info` → `/measurements` → `/results` → `/dashboard`

## 🧪 Тестирование без backend

Если бэкенд еще не готов, можете временно использовать моковые данные:

В `src/lib/api.ts` замените:
```typescript
calculateDailyCalories: async (data: OnboardingData) => {
  // Мок для тестирования
  await new Promise(resolve => setTimeout(resolve, 2000));
  return {
    dailyCalories: 2400,
    bmr: 1750,
    tdee: 2400,
    recommendedProtein: 150,
    recommendedCarbs: 240,
    recommendedFat: 80,
  };
},
```

## 📚 Документация

Подробная документация доступна в файле `BACKEND_INTEGRATION.md`

---

**Готово к использованию!** 🎉
