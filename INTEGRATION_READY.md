# ✅ Интеграция завершена!

## 🎯 Что сделано:

### 1. **API интегрирован с вашим backend**
- URL: `http://localhost:5210/api/CalorieCalculator/calculate`
- Все данные маппятся в правильный формат

### 2. **Маппинг значений:**

#### Gender:
- `male` → `0`
- `female` → `1`

#### Activity Level:
- `Not very active` → `0`
- `Lightly active` → `1`
- `Active` → `2`
- `Very active` → `3`

#### Goal:
- `Lose weight` → `0`
- `Maintain weight` → `1` (goalWeightKg = null)
- `Gain weight` → `2`

#### Дата:
- Формат: `YYYY-MM-DD` (например: `2000-01-15`)

### 3. **Что происходит:**

**Онбординг собирает данные:**
```
Goals → Activity Level → Personal Info → Measurements → Results
```

**Отправляется на API:**
```json
{
  "goal": 0,
  "activityLevel": 1,
  "gender": 0,
  "birthDate": "2000-01-15",
  "heightCm": 175.5,
  "weightKg": 75.0,
  "goalWeightKg": 70.0
}
```

**Получает ответ:**
```json
{
  "dailyCalories": 2020,
  "unitLabel": "калории",
  "planText": "Снижать вес",
  "goal": "LoseWeight"
}
```

**Показывает результат пользователю** ✨

## 🚀 Как протестировать:

1. **Запустите backend:**
```bash
# Убедитесь что ваш API запущен на http://localhost:5210
```

2. **Запустите frontend:**
```bash
npm run dev
```

3. **Пройдите онбординг:**
- Перейдите: `http://localhost:5173/register`
- Зарегистрируйтесь
- Пройдите все шаги онбординга
- На странице Results увидите данные от вашего API!

## 🔍 Проверка данных:

Откройте DevTools → Console, увидите логи:
```
Selected activity level: Lightly active ->  1
Gender: male ->  0
Birthdate: 2000-01-15
Measurements: { goal: 0, heightInCm: 175.5, weightInKg: 75, goalWeightInKg: 70 }
```

## 📝 Важные файлы:

- **Типы:** `src/types/index.ts`
- **API:** `src/lib/api.ts`
- **Маппинг:** `src/lib/onboardingMappers.ts`
- **Контекст:** `src/contexts/OnboardingContext.tsx`
- **Конфиг:** `.env`

## ⚠️ Если что-то не работает:

1. Проверьте что backend запущен: `http://localhost:5210`
2. Проверьте консоль браузера на ошибки
3. Проверьте Network tab → XHR запросы
4. Убедитесь что прошли ВСЕ шаги онбординга

---

**Готово к тестированию!** 🎉
